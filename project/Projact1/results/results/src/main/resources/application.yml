server:
  port: 3006

spring:
  application:
    name: "Result"

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    eureka.healthcheck.enabled: true;
    service-url:
      default:zone: http://localhost:8761/eureka/
    instance:
      hostname: localhost